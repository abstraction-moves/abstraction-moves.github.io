let s = null
Display(Var('buckets'))

// Revisualize(Select(buckets), 'Graph', { pointer_graph: true })
// Revisualize(Select(buckets), 'Sequence', { style: 'Blocks', orientation: 'Vertical' })

// s = SelectByCondition(buckets, (v) => v == null)
// Clump(s)

// s = Select(...buckets, { resolve_pointers: false })
// LabelLocation(s, { placement: 'left', show_braces: false })
