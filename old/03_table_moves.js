Display(Var('T'))
// Display(Var('T'), { visual: 'Table' })

// let s = DataSelect(T.slice(5, 6))
// s = Merge(s)
// Abbreviate(s)
// Remove(s)

// // Get the first column
// let C = []
// for (let i = 0; i < T.length; i++) {
//     C.push(T[i]['site'])
// }
